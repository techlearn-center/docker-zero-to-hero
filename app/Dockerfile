# ============================================
# Docker Zero-to-Hero: Dockerfile Challenge
# ============================================
# YOUR TASK (Module 04): Containerize the todo app
#
# Requirements:
# 1. Use node:18-alpine as base image
# 2. Set working directory to /app
# 3. Copy package.json first (for layer caching)
# 4. Install production dependencies
# 5. Copy the rest of the source code
# 6. Expose port 3000
# 7. Define the start command: node src/index.js
#
# See 04-containerize-application/README.md for step-by-step guidance.
# ============================================

# TODO: Replace everything below with your Dockerfile!
# Start with: FROM node:18-alpine

FROM node:18-alpine
WORKDIR /app
# TODO: Add the remaining instructions to complete the Dockerfile
