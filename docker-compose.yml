# ============================================
# Docker Zero-to-Hero: Docker Compose Challenge
# ============================================
# YOUR TASK (Module 10): Define a multi-container app
#
# Requirements:
# 1. Define a "web" service using ./app as build context
# 2. Map port 3000:3000
# 3. Set environment variables for MySQL connection
# 4. Define a "mysql" service using mysql:8.0 image
# 5. Set MySQL environment variables (root password, database)
# 6. Add a named volume for MySQL data persistence
# 7. Ensure the web service depends on mysql
#
# See 10-use-docker-compose/README.md for step-by-step guidance.
# ============================================

# TODO: Replace everything below with your docker-compose.yml!

services:
  # TODO: Define the "web" service
  #   - build: ./app
  #   - ports: 3000:3000
  #   - environment: MYSQL_HOST, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DB
  #   - depends_on: mysql

  # TODO: Define the "mysql" service
  #   - image: mysql:8.0
  #   - environment: MYSQL_ROOT_PASSWORD, MYSQL_DATABASE
  #   - volumes: mysql-data:/var/lib/mysql

# TODO: Define named volumes
# volumes:
#   mysql-data:
